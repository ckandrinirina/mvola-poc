---
name: help
description: >
  Quick reference for all available project skills. Shows commands,
  workflow order, and usage examples. Use when unsure which skill to run.
---

# Project Skills — Quick Reference

## Workflow Order

```
1. /design   → Refine spec into architecture docs
2. /team     → Generate expert + guide skills
3. /plan     → Create epics, stories, roadmap
4. /publish  → Push epics/stories to GitHub Issues
5. /track    → Check progress, find next story
6. /build    → Implement a story (TDD + QA)
7. /fix      → Fix a bug in a story (diagnose + TDD fix)
8. /ship     → Commit, PR, and update GitHub Issues
```

## Commands

### /design [spec-file]
Refine a specification into detailed architecture docs in `docs/architecture/`.
Asks conversational questions to fill gaps. Never modifies the original spec.

```
/design docs/specifications.md
/design docs/new-feature.md          # Also works for adding features
```

**Output:** `docs/architecture/` (overview, folder-structure, tech-stack, components, data-flow, api-contracts, database-schema, configuration, dev-guide)

---

### /team [--regenerate]
Generate project-tailored expert and guide skills from `docs/architecture/`.
Researches current best practices via context7 before generating.

```
/team                                # First-time generation
/team --regenerate                   # Refresh after architecture changes
```

**Output:**
- `.claude/skills/experts/` — frontend, backend, qa, analyst, devops, qa-project
- `.claude/skills/guides/` — one per language/framework (rust, cpp, react-native, etc.)

---

### /plan [spec-file]
Generate epics, stories, roadmap from a specification. Creates structured `tasks/` folder.

```
/plan docs/specifications.md         # Full project plan
/plan docs/new-feature.md            # Feature-scoped plan
```

**Output:** `tasks/YYYY-MM-DD_<project>/` with epics, stories, and roadmap

---

### /publish [tasks-folder]
Publish epics and stories to GitHub Issues with labels and cross-references.

```
/publish tasks/2026-04-13_ck-synth   # Publish specific plan
/publish                             # Auto-detect if only one plan exists
```

**Output:** GitHub Issues with `epic`, `story`, `size/*` labels

---

### /track [status|next|progress]
Show project progress dashboard.

```
/track                               # Full status dashboard
/track next                          # Suggest next story to implement
/track progress                      # Epic completion percentages
```

---

### /build [story-path]
Implement a story using TDD with SOLID principles. Auto-loads relevant expert and guide skills.

```
/build tasks/.../stories/01_setup.md  # Implement specific story
/build                                # Interactive story picker
```

**Workflow:** Tests first (red) → Implement (green) → Refactor → QA validation → Commit

---

### /fix [story-path]
Fix a bug tied to an existing story. QA reproduces the bug first, then applies a minimal TDD fix.

```
/fix tasks/.../stories/01_setup.md    # Fix bug in specific story
/fix                                  # Interactive story picker
```

**Workflow:** Select story → Describe bug → QA reproduces → Plan minimal fix → TDD fix (red→green) → QA validates → Ship

---

### /ship [story-path]
Commit changes, create PR, and update linked GitHub Issues. Auto-detects story context from branch name or file path.

```
/ship tasks/.../stories/01_setup.md   # With story context (links to issues)
/ship                                 # Standalone commit (no story)
```

**Does:** Stage files → Commit (conventional format) → PR (optional) → Close/comment on story issue → Update epic checklist

---

## Expert Skills (generated by /team)

| Command | Role |
|---------|------|
| `/expert-frontend` | Frontend development |
| `/expert-backend` | Backend development |
| `/expert-qa` | Testing and QA |
| `/expert-analyst` | Code review and analysis |
| `/expert-devops` | CI/CD and infrastructure |
| `/expert-qa-project` | Answer questions about the project |

## Guide Skills (auto-loaded, generated by /team)

Language and framework best-practice guides. Claude loads these automatically
when working with the relevant technology. No manual invocation needed.

Examples: `guide-rust`, `guide-cpp`, `guide-react-native`, `guide-axum`

---

## Typical First-Time Setup

```
/design docs/specifications.md       # 1. Generate architecture docs
/team                                # 2. Create expert + guide skills
/plan docs/specifications.md         # 3. Generate epics and stories
/publish                             # 4. (Optional) Push to GitHub Issues
/track next                          # 5. Find first story to implement
/build                               # 6. Start building!
```

## Adding a Feature Later

```
/design docs/new-feature.md          # 1. Extend architecture docs
/team --regenerate                   # 2. Refresh experts with new context
/plan docs/new-feature.md            # 3. Generate feature epics/stories
/build                               # 4. Start implementing
```

## Fixing a Bug

```
/fix tasks/.../stories/01_setup.md   # Fix bug in a known story
/fix                                 # Pick from implemented stories
```

## Shipping Changes

```
/ship tasks/.../stories/01_setup.md  # Commit + PR + update issues
/ship                                # Standalone commit (no story)
```
